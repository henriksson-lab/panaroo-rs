name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

# A new push to the same branch supersedes the previous run.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # The user-facing requirement: the project must compile on Linux, macOS and Windows.
  #
  # `build.rs` compiles the vendored C++ in `vendor/edlib/edlib.cpp` through the `cc`
  # crate. No extra setup is needed for that on any runner: ubuntu-latest has gcc/g++,
  # macos-latest has the Xcode command line tools (clang++), and windows-latest ships
  # the MSVC build tools, which `cc` locates itself for the default
  # x86_64-pc-windows-msvc target. `-std=c++11` is passed via `flag_if_supported`, so
  # MSVC simply ignores it.
  check:
    name: check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            run_tests: true
          - os: macos-latest
            run_tests: true
          - os: windows-latest
            run_tests: true
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and target dir
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.os }}

      # Compiles the library, the binary, the examples and the test harnesses --
      # without running any of them. This is the whole job on Windows.
      # `cli` only, NOT `--all-features`. BOTH embedded backends are currently path
      # dependencies on local checkouts -- that is where their latest code lives (the
      # cdhit-rs `-T` parallelisation fix and the rust-MAFFT DNA gap-scale fix are neither
      # of them pushed) -- so neither resolves on a runner. Re-add `cdhit-embedded` /
      # `mafft-embedded` here as each is pushed and its dependency returns to `git`.
      # The binary target is behind `cli`, so it must be on or `src/main.rs` is never built.
      - name: cargo check --all-targets --features cli
        run: cargo check --all-targets --features cli

      # Tests now run on all three platforms.
      #
      # They previously could not on Windows: `support/proc.rs` reproduced Python's
      # `subprocess.run(..., shell=True)` by spawning `/bin/sh -c`, which does not exist
      # there. It now goes through `proc::shell`, which picks `/bin/sh -c` on Unix and
      # `cmd /C` on Windows -- both of which are what CPython's `shell=True` actually
      # spawns on those platforms -- and the two tests that assumed a Unix shell (`true`
      # is not a `cmd` builtin; `cmd`'s echo emits CRLF) were made portable.
      #
      # NOTE: the parity and benchmark suites under `tests/` are still Unix-only and are
      # NOT run here by any platform -- they need conda, cd-hit and mafft. This job covers
      # the library's own unit tests.
      - name: cargo test
        if: matrix.run_tests
        run: cargo test --features cli

  # Style and lint gates run once, on Linux only -- they are platform-independent.
  lint:
    name: lint (clippy + rustfmt)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust (stable) with clippy and rustfmt
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          key: lint

      - name: cargo clippy
        run: cargo clippy --all-targets --features cli -- -D warnings

      - name: cargo fmt --check
        run: cargo fmt --check

  # The MSRV in Cargo.toml was derived analytically from the dependency graph via
  # `cargo metadata` and has never actually been compiled against. This job is the
  # first real test of it. The version is read out of the manifest at run time rather
  # than duplicated here, so bumping `rust-version` bumps what CI tests.
  msrv:
    name: MSRV (rust-version from Cargo.toml)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Read rust-version from Cargo.toml
        id: msrv
        run: |
          version="$(grep -m1 -E '^[[:space:]]*rust-version[[:space:]]*=' Cargo.toml \
            | sed -E 's/.*"([^"]+)".*/\1/')"
          if [ -z "$version" ]; then
            echo "::error file=Cargo.toml::no rust-version key found in [package]"
            exit 1
          fi
          echo "Declared MSRV: $version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Install Rust ${{ steps.msrv.outputs.version }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.msrv.outputs.version }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: msrv-${{ steps.msrv.outputs.version }}

      # `cli` only, NOT `--all-features`. BOTH embedded backends are currently path
      # dependencies on local checkouts -- that is where their latest code lives (the
      # cdhit-rs `-T` parallelisation fix and the rust-MAFFT DNA gap-scale fix are neither
      # of them pushed) -- so neither resolves on a runner. Re-add `cdhit-embedded` /
      # `mafft-embedded` here as each is pushed and its dependency returns to `git`.
      # The binary target is behind `cli`, so it must be on or `src/main.rs` is never built.
      - name: cargo check --all-targets --features cli
        run: cargo check --all-targets --features cli
